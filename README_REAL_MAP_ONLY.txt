COMMANDCENTER V2.18.3 REAL LEAFLET MAP ONLY PATCH

This patch replaces ONLY:
- app.js
- index.html

Purpose:
- Removes the forced fake SVG USA blue-blob injector from app.js.
- Removes the static fake SVG block from index.html.
- Adds a final renderHeatMap override that renders a real Leaflet/OpenStreetMap USA map with location pins.

Do not upload the old V2.18.1 patch again.
