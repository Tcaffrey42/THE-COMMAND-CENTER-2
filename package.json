{
  "name": "commandcenter-v2-13-5",
  "version": "2.13.5",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src 2>/dev/null || mkdir -p dist/src && cp app.js styles.css env.js dist/src/ 2>/dev/null || true"
  },
  "engines": {
    "node": "20.x"
  }
}
