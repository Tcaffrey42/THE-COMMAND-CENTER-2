{
  "name": "commandcenter-v2-13-5",
  "version": "2.13.5",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/src && cp index.html dist/index.html && cp app.js styles.css env.js dist/src/"
  },
  "engines": {
    "node": "20.x"
  }
}
