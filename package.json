{
  "name": "commandcenter-v2-14-stable-root-static",
  "version": "2.14.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R index.html styles.css app.js *.png *.jpg *.jpeg *.webp *.svg env.js dist/ 2>/dev/null || true"
  },
  "engines": {
    "node": "20.x"
  }
}