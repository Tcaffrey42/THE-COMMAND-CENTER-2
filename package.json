{
  "name": "commandcenter-v2-13-vercel-no-script-build",
  "version": "2.13.4",
  "private": true,
  "scripts": {
    "build": "echo Using prebuilt static dist",
    "start": "npx serve dist",
    "preview": "npx serve dist"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "serve": "latest"
  }
}
