{
  "name": "commandcenter-v2-14-pm-planner",
  "version": "2.14.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src"
  },
  "engines": {
    "node": "20.x"
  }
}
