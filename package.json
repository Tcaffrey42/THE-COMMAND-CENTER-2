{
  "name": "commandcenter-v2-16-auth-roles-src-fixed",
  "version": "2.16.1",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R index.html styles.css *.js *.png *.jpg *.jpeg *.webp *.svg *.sql README.md dist/ 2>/dev/null || true"
  },
  "engines": {
    "node": "20.x"
  }
}
