{
  "name": "commandcenter-v2-13-5-vercel-no-scripts-folder",
  "version": "2.13.5",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R index.html src dist/",
    "start": "npx serve dist",
    "preview": "npx serve dist"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "serve": "latest"
  }
}
