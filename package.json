{
  "name": "commandcenter-v2-16-1-auth-roles",
  "version": "2.16.1",
  "private": true,
  "description": "CC CommandCenter V2.16.1 — multi-file static app (Supabase auth + roles, Leaflet map) deployed to Vercel.",
  "scripts": {
    "dev": "serve . -l 3000",
    "build": "mkdir -p dist && cp -R index.html styles.css app.js auth.js env.js *.png *.svg *.sql README.md dist/ 2>/dev/null || true",
    "start": "serve . -l 3000",
    "preview": "serve dist -l 3000"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "serve": "latest"
  }
}
