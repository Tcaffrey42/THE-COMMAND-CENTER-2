COMMANDCENTER V2.18.2 MAP-ONLY HTML PATCH

This ZIP contains ONLY:
- index.html
- this readme

It does NOT contain app.js.
It does NOT contain styles.css.
It does NOT contain env.js.
It does NOT overwrite the core app logic.

What it changes:
- Removes the fake SVG USA map block.
- Puts the real Leaflet map container inside the Heat Map page.
- Adds a small inline map initializer.
- Adds 3 demo pins: red, yellow, green.

Use this only on the restored working base.
